# More info: https://docs.docker.com/engine/reference/builder/#dockerignore-file
# Ignore everything by default and re-include only needed files
**

# Re-include Go source files (but not *_test.go)
!**/*.go
**/*_test.go

# Re-include Go module files
!go.mod
!go.sum

# Re-include embedded resources referenced by //go:embed directives.
# Without this the Docker build fails with "pattern templates/...: no matching
# files found" because the allow-list above only re-included *.go.
!**/templates/**
